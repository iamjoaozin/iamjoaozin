import { mockSubreddits } from './subreddits.mock';
import { mockJobs } from './jobs.mock';
import { mockImages } from './images.mock';
import { mockDrafts } from './drafts.mock';
// To be added: templates

export const MOCK_DB = {
  subreddits: mockSubreddits,
  jobs: mockJobs,
  images: mockImages,
  drafts: mockDrafts,
};
